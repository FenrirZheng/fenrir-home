#+TITLE: Related Git Repositories
#+DESCRIPTION: Independent git repos that live under $HOME alongside the dotfiles repo.
#+DESCRIPTION: Previously tracked as submodules; now standalone clones.
#+DESCRIPTION: The parent dotfiles repo does NOT track these — since
#+DESCRIPTION: =status.showUntrackedFiles=normal= (2026-09-02) they show up as untracked
#+DESCRIPTION: entries in its =git status=.

* Config repos

| local path               | remote                                          | description                          | build / notes                      |
|---------------------------+-------------------------------------------------+--------------------------------------+------------------------------------|
| =~/.claude/=              | =git@github.com:FenrirZheng/claude-for-fenrir.git= | Claude Code config + skills          | pure config, no build              |
| =~/.tmux/=                | =git@github.com:FenrirZheng/config-tmux.git=       | tmux config + Rust toolset + plugins | =cargo build --release= in =tools/= |
| =~/.emacs.d/=             | =git@github.com:FenrirZheng/emacs-config.git=      | Emacs config                         | pure config                        |
| =~/.config/=              | =git@github.com:FenrirZheng/linux-user-home-.config.git= | =$HOME/.config= itself; owns the whole tracked =.config= tree — =openfortivpn/=, =alacritty/=, =autostart/=, =environment.d/=, =fcitx5/=, =keyd/=, =systemd/user/= (18 files) | Keep private — tracked =openfortivpn/config= holds a plaintext VPN password. No root =.gitignore=, so untracked siblings stay visible. keyd deploy: =sudo cp keyd/default.conf /etc/keyd/default.conf && sudo systemctl restart keyd=. Units under =systemd/user/= are =systemctl --user= managed |
| =~/code/nicro-skill/=     | =git@github.com:FenrirZheng/nicro-skills.git=      | =nicro= Claude Code plugin (17 skills) | =claude plugin marketplace add ~/code/nicro-skill= then enable; =~/.claude/settings.json= references this path |

The six per-app repos that used to sit inside =~/.config/= (=alacritty=,
=autostart=, =environment.d=, =fcitx5=, =keyd=, =systemd=) were absorbed into the
=~/.config/= repo on 2026-09-03: they are ordinary tracked directories now, not
clones, and have no rows of their own. Their old histories are archived as
=git bundle= files in =~/config-repo-archive/=.

* CLI tool repos

| local path                            | remote                                                | description                        | build                              | symlink                                    |
|---------------------------------------+-------------------------------------------------------+------------------------------------+------------------------------------+--------------------------------------------|
| =~/fenrir-tools/claud-chat-acp/=      | =git@github.com:FenrirZheng/claud-chat-acp.git=      | Rust ACP client (=claud-chat= bin) | =cargo build --release=            | =~/.local/bin/claud-chat= → =target/release/claud-chat= |
| =~/fenrir-tools/claude-agentic-chat/= | =git@github.com:FenrirZheng/claude-agentic-chat.git= | Node Claude Agent SDK chat         | =npm install && npm run build=     | =~/.local/bin/claude-chat= → =dist/index.js= |

* Unrelated clones (not managed by this file)

Repos under =~/code/=, =~/WebstormProjects/=, =~/Documents/= are independent
clones unrelated to the dotfiles repo. They are not listed here because they
have no operational relationship with the home-directory config. Exception:
=~/code/nicro-skill/= is listed above — =~/.claude/settings.json= points at it
as a plugin marketplace, so it is operationally part of the config.
